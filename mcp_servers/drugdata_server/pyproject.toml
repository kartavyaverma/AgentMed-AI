[project]
name = "mcp-drugdata-server"
version = "0.1.0"
description = "MCP server exposing drug info, interactions, and contraindications"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.1.0,<2.0.0",
    "httpx>=0.27.0",
]
