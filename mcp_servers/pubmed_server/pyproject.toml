[project]
name = "mcp-pubmed-server"
version = "0.1.0"
description = "MCP server exposing PubMed literature search as a tool"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.1.0,<2.0.0",
    "biopython>=1.84",
    "httpx>=0.27.0",
]
